{
  "name": "@tamem/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@tamem/types": "workspace:*",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@tamem/eslint-config": "workspace:*",
    "@tamem/tsconfig": "workspace:*",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
