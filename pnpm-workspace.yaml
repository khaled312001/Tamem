packages:
  - 'apps/*'
  - 'packages/*'

# Approved build scripts (trusted dependencies — required for native modules + DB tooling)
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  '@scarf/scarf': false
  esbuild: true
  msw: true
  prisma: true
  sharp: true
