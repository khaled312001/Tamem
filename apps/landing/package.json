{
  "name": "@tamem/landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 4321 --host",
    "build": "astro check && astro build",
    "preview": "astro preview --port 4321",
    "lint": "eslint src --max-warnings=0 || true",
    "typecheck": "astro check",
    "test": "echo 'no tests for landing yet' && exit 0",
    "clean": "rimraf dist .astro .turbo"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.2.1",
    "@astrojs/tailwind": "^5.1.4",
    "@tamem/ui": "workspace:*",
    "astro": "^5.1.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@tamem/eslint-config": "workspace:*",
    "@tamem/tsconfig": "workspace:*",
    "rimraf": "^6.0.1"
  }
}
