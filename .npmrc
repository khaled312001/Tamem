; pnpm config for Tamem monorepo — Expo needs many transitive deps hoisted
; so Metro's CommonJS resolver can find them in the standard node_modules.

shamefully-hoist=true
node-linker=hoisted

; Avoid pnpm asking to confirm node_modules purge in scripts/CI
confirm-modules-purge=false
